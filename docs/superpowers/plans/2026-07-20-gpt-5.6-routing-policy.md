# GPT-5.6 Routing Policy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make all 13 Amplifier roles resolve deterministically across full, partial, and emergency model catalogs while preserving explicit model overrides.

**Architecture:** Keep account-aware selection in `routing/openai-chatgpt.yaml` and leave provider payload construction unchanged. Add a test-only deterministic resolver that exercises the shipped YAML against synthetic catalogs, then synchronize the README with the tested policy.

**Tech Stack:** Python 3.11+, pytest, PyYAML supplied through the existing Amplifier development environment, YAML routing configuration, Markdown.

## Global Constraints

- Modify only PR #8 on `release/gpt-5.6-provider-recovery`.
- Do not create another branch or pull request.
- Do not add provider-side autonomous model selection.
- Do not collect account, subscription, token, or private catalog data.
- Do not introduce `gpt-5.6-codex`.
- Preserve explicit `request.model` precedence.

---

### Task 1: Add routing policy contract tests

**Files:**
- Create: `tests/test_routing.py`

**Interfaces:**
- Consumes: `routing/openai-chatgpt.yaml` with role entries containing `candidates`.
- Produces: `_resolve(role: dict, catalog: list[str]) -> dict` for deterministic test-only catalog resolution.

- [ ] **Step 1: Write the failing tests**

Create tests that load the YAML with `yaml.safe_load`, assert the exact 13-role
set and candidate lists, reject `gpt-5.6-codex`, and resolve every role against:

```python
FULL_CATALOG = [
    "gpt-5.6-sol",
    "gpt-5.6-terra",
    "gpt-5.6-luna",
    "gpt-5.5-codex",
]
PARTIAL_CATALOG = ["gpt-5.6-terra", "gpt-5.5"]
EMERGENCY_CATALOG = ["gpt-5.5"]
```

The fast-role assertion must require:

```python
[
    ("gpt-5.6-luna", None),
    ("gpt-5.6-terra", None),
    ("gpt-5.6-sol", None),
    ("gpt-5.*", None),
]
```

- [ ] **Step 2: Run tests to verify RED**

Run: `UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/test_routing.py -v`

Expected: FAIL because the shipped `fast` role currently aliases the Sol-first
general policy.

- [ ] **Step 3: Keep the failing test unchanged for Task 2**

Do not weaken the candidate-order assertion. The YAML is the implementation
surface that must change.

### Task 2: Implement the tested routing policy

**Files:**
- Modify: `routing/openai-chatgpt.yaml`
- Test: `tests/test_routing.py`

**Interfaces:**
- Consumes: exact candidate order defined in the design.
- Produces: a separate `fast` role with Luna-first routing and accurate policy comments.

- [ ] **Step 1: Add the minimal YAML change**

Replace `fast: *general` with an explicit candidate list ordered Luna, Terra,
Sol, and `gpt-5.*`. Update the header comment to distinguish public family
positioning from project routing policy and to describe the actual reasoning
and critique routes accurately.

- [ ] **Step 2: Run tests to verify GREEN**

Run: `UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/test_routing.py -v`

Expected: all routing contract tests pass.

- [ ] **Step 3: Run the explicit override regression**

Run: `UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/test_provider.py::TestPayloadBuilding::test_request_model_overrides_default -v`

Expected: PASS, proving routing work did not alter explicit provider overrides.

### Task 3: Synchronize README rationale and examples

**Files:**
- Modify: `README.md`
- Test: `tests/test_routing.py`

**Interfaces:**
- Consumes: tested routing order from `routing/openai-chatgpt.yaml`.
- Produces: user-facing rationale that identifies public facts versus repository policy.

- [ ] **Step 1: Add a failing documentation synchronization assertion**

Extend `tests/test_routing.py` to require the README routing section to state
that fast prefers Luna, general roles prefer Sol, coding uses catalog Codex
matching, and explicit model requests win.

- [ ] **Step 2: Run the test to verify RED**

Run: `UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/test_routing.py -v`

Expected: FAIL because the current README does not state the Luna-first fast
policy.

- [ ] **Step 3: Make the minimal README update**

Update the routing section with the exact tested order and explain that the
model-family descriptions come from OpenAI while candidate order is repository
policy. Change the local bundle example from `gpt-5.5` to the documented
`gpt-5.6-sol` default.

- [ ] **Step 4: Run tests to verify GREEN**

Run: `UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/test_routing.py -v`

Expected: all routing and documentation synchronization tests pass.

### Task 4: Verify and publish the CODY-9 increment

**Files:**
- Verify all files changed for CODY-9 and the pre-existing PR #8 changes.

**Interfaces:**
- Consumes: repository test and formatting commands.
- Produces: one evidence-backed CODY-9 commit on PR #8's existing head branch.

- [ ] **Step 1: Run focused and full verification**

Run:

```bash
UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/test_routing.py -v
UV_CACHE_DIR=/tmp/cody9-uv-cache uv run pytest tests/ -v
UV_CACHE_DIR=/tmp/cody9-uv-cache uv run ruff check .
UV_CACHE_DIR=/tmp/cody9-uv-cache uv run ruff format --check .
git diff --check
```

Expected: zero test failures, zero lint errors, no formatting changes required,
and no whitespace errors.

- [ ] **Step 2: Review scope**

Confirm CODY-9 changes are limited to the design/plan records,
`tests/test_routing.py`, `routing/openai-chatgpt.yaml`, and `README.md`. Exclude
uncommitted local representations of already-published CODY-6, CODY-8, and
CODY-16 work from the CODY-9 publication.

- [ ] **Step 3: Publish to the existing PR branch**

Re-resolve PR #8's exact head SHA, create one commit containing only the CODY-9
files, and advance `release/gpt-5.6-provider-recovery` with an exact lease. Do
not create or merge another PR.

- [ ] **Step 4: Record evidence**

Update CODY-9 with the exact commit SHA, test counts, lint/format results, and
CI conclusion. Set CODY-9 Done only after all acceptance criteria pass.
